using System;

namespace HyperMapper
{
    public static class ObjectMapper
    {
        public static T Clone<T>(T cloneFrom)
        {
            // return StandardObjectMapper.Instance.GetMapper<T, T>().Map(cloneFrom, StandardObjectMapper.Instance);
            throw new NotImplementedException();
        }
    }
}
